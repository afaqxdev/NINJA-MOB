import 'package:flutter/material.dart';

class AppColor {
  Color mainColor = Colors.deepPurple.shade200;
  Color mainColo = Color.fromARGB(255, 212, 192, 248);

  Color white = Color.fromARGB(230, 248, 250, 250);
  Color buttonColor = Color.fromARGB(158, 90, 48, 99);
  Color grey = Colors.grey;
  Color black = Colors.black;
}
